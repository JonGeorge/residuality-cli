[package]
name = "residuality"
version = "0.1.0"
edition = "2024"
default-run = "residuality"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
csv = "1.4.0"
serde = { version = "1", features = ["derive"] }
